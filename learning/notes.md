
regular javascript functions=

function name(paramerters){
    return value;
}

arrow functions=

const name = (parameters) => {
    return value;
}

its still a fucntion, but = assigns the varble to a function instead of a value.
